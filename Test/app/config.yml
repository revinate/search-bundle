imports:
    - { resource: parameters.yml }

framework:
    secret: "Revinate Search Test"
    test: ~

revinate_search:
    connection: { host: 'elasticsearch', port: 9200 }
    paths:  ['Test/Entity']
